import { ReadableHistoryPipe } from './readable-history.pipe';

describe('ReadableHistoryPipe', () => {
  it('create an instance', () => {
    const pipe = new ReadableHistoryPipe();
    expect(pipe).toBeTruthy();
  });
});
