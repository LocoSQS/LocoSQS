import { IconHistoryPipe } from './icon-history.pipe';

describe('IconHistoryPipe', () => {
  it('create an instance', () => {
    const pipe = new IconHistoryPipe();
    expect(pipe).toBeTruthy();
  });
});
